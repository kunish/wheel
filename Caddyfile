:3000 {
	# /v1/* and /api/* -> worker (direct, no buffering)
	handle /v1/* {
		reverse_proxy worker:8787
	}

	handle /api/* {
		reverse_proxy worker:8787
	}

	# Everything else -> Vite SPA static files
	handle {
		reverse_proxy web:3000
	}
}
